From 6d614058b07863950b7c11f66e22a41e8fcfffa0 Mon Sep 17 00:00:00 2001
From: Pavel Shirshov <pavelsh@microsoft.com>
Date: Mon, 28 Sep 2020 16:36:09 -0700
Subject: [PATCH 1/1] Allow BGP attr NEXT_HOP to be 0.0.0.0 due to alleviate
 the vendor bug

---
 bgpd/bgp_route.c | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

diff --git a/bgpd/bgp_route.c b/bgpd/bgp_route.c
index 96fbcad02..fc482e429 100644
--- a/bgpd/bgp_route.c
+++ b/bgpd/bgp_route.c
@@ -3250,8 +3250,7 @@ bool bgp_update_martian_nexthop(struct bgp *bgp, afi_t afi, safi_t safi,
 
 	/* If NEXT_HOP is present, validate it. */
 	if (attr->flag & ATTR_FLAG_BIT(BGP_ATTR_NEXT_HOP)) {
-		if ((attr->nexthop.s_addr == INADDR_ANY && !is_bgp_static_route)
-		    || IPV4_CLASS_DE(ntohl(attr->nexthop.s_addr))
+		if (IPV4_CLASS_DE(ntohl(attr->nexthop.s_addr))
 		    || bgp_nexthop_self(bgp, afi, type, stype, attr, rn))
 			return true;
 	}
-- 
2.17.1.windows.2

