diff --git a/common/discover.c b/common/discover.c
index 53afecc..5a0fa32 100644
--- a/common/discover.c
+++ b/common/discover.c
@@ -462,7 +462,7 @@ next_iface(struct iface_info *info, int *err, struct iface_conf_list *ifaces) {
 		return 0;
 	}
 	memset(info, 0, sizeof(struct iface_info));
-	strncpy(info->name, ifaces->next->ifa_name, sizeof(info->name) - 1);
+	strncpy(info->name, ifaces->next->ifa_name, sizeof(info->name));
 	memset(&info->addr, 0 , sizeof(info->addr));
 	/*
 	 * getifaddrs() can on Linux with some interfaces like PPP or TEQL
