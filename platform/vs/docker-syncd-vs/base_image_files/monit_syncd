###############################################################################
## Monit configuration for syncd container
###############################################################################
check program container_memory_syncd with path "/usr/bin/memory_checker syncd"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
