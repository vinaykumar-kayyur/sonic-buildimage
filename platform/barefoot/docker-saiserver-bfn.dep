
DPATH       := $($(DOCKER_SAISERVER_BFN)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) platform/barefoot/docker-saiserver-bfn.mk platform/barefoot/docker-saiserver-bfn.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_SAISERVER_BFN)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_SAISERVER_BFN)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SAISERVER_BFN)_DEP_FILES   := $(DEP_FILES)
