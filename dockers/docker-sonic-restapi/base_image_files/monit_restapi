###############################################################################
## Monit configuration for restapi container
###############################################################################
check program container_memory_restapi with path "/usr/bin/memory_checker restapi"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
