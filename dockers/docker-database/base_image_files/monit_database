###############################################################################
## Monit configuration for database container
###############################################################################
check program container_memory_database with path "/usr/bin/memory_checker database"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
