###############################################################################
## Monit configuration for teamd container
###############################################################################
check program container_memory_teamd with path "/usr/bin/memory_checker teamd"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
