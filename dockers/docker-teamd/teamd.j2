{
    "device": "{{ name }}",
    "runner": {
        "name": "lacp",
        "active": true,
        "min_ports": 2,
        "tx_hash": ["eth", "ipv4", "ipv6"]
    },
    "link_watch": {
        "name": "ethtool"
    },
    "ports": {
        {% for member in members %}
        "{{member}}": {}{% if not loop.last %},
        {% endif %}
        {% endfor %}
    }
}

