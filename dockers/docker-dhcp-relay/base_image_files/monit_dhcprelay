###############################################################################
## Monit configuration for dhcp_relay container
###############################################################################
check program container_memory_dhcp_relay with path "/usr/bin/memory_checker dhcp_relay"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
