###############################################################################
## Monit configuration for nat container
###############################################################################
check program container_memory_nat with path "/usr/bin/memory_checker nat"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles

