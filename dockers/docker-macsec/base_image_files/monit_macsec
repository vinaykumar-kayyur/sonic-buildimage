###############################################################################
## Monit configuration for macsec container
###############################################################################
check program container_memory_macsec with path "/usr/bin/memory_checker macsec"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
