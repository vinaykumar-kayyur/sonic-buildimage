###############################################################################
## Monit configuration for sflow container
###############################################################################
check program container_memory_sflow with path "/usr/bin/memory_checker sflow"
    if status == 3 for 10 times within 20 cycles then alert repeat every 1 cycles
