
SPATH       := $($(USBMOUNT)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/usbmount.mk rules/usbmount.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(USBMOUNT)_CACHE_MODE  := GIT_CONTENT_SHA 
$(USBMOUNT)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(USBMOUNT)_DEP_FILES   := $(DEP_FILES)

