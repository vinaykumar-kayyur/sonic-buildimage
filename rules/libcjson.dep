
SPATH       := $($(LIBCJSON)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/libcjson.mk rules/libcjson.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(LIBCJSON)_CACHE_MODE  := GIT_CONTENT_SHA
$(LIBCJSON)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(LIBCJSON)_DEP_FILES   := $(DEP_FILES)

