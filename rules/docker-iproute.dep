DPATH       := $($(DOCKER_IPROUTE)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-iproute.mk rules/docker-iproute.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_IPROUTE)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_IPROUTE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_IPROUTE)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_IPROUTE),$(DOCKER_IPROUTE_DBG)))
