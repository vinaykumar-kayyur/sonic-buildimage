#DPKG FRK

SPATH       := $($(GENL_PACKET_DEV)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/genl-packet.mk rules/genl-packet.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(GENL_PACKET_DEV)_CACHE_MODE  := GIT_CONTENT_SHA
$(GENL_PACKET_DEV)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(GENL_PACKET_DEV)_DEP_FILES   := $(DEP_FILES)
