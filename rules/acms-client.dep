
SPATH       := $($(ACMS_CLIENT)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/acms-client.mk rules/acms.client.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(addprefix $(SPATH)/,$(shell cd $(SPATH) && git ls-files |grep -Ev '^bond$$$$'))

$(ACMS_CLIENT)_CACHE_MODE  := GIT_CONTENT_SHA
$(ACMS_CLIENT)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(ACMS_CLIENT)_DEP_FILES   := $(DEP_FILES)
