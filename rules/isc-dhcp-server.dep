
SPATH       := $($(ISC_DHCP_SERVER)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/isc-dhcp-server.mk rules/isc-dhcp-server.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(ISC_DHCP_SERVER)_CACHE_MODE  := GIT_CONTENT_SHA 
$(ISC_DHCP_SERVER)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(ISC_DHCP_SERVER)_DEP_FILES   := $(DEP_FILES)

