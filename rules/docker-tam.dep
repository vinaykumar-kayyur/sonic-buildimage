
DPATH       := $($(DOCKER_TAM)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-tam.mk rules/docker-tam.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(SONIC_COMMON_DOCKER_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_TAM)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_TAM)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_TAM)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_TAM),$(DOCKER_TAM_DBG)))
