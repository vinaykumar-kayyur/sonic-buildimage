
SPATH       := $($(BOND)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/bond.mk rules/bond.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(BOND)_CACHE_MODE  := GIT_CONTENT_SHA
$(BOND)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(BOND)_DEP_FILES   := $(DEP_FILES)

